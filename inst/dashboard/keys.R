# keys for socraticswirl-instructor - login to database is handled in the program

Sys.setenv(PARSE_APPLICATION_ID = "<parse.com application ID>", 
PARSE_API_KEY = "<parse.com API key>")
if (username == "default") {
  username="<socratic swirl instructor username>"
}
if (password == "default") {
password = "<socratic swirl instructor password>"
}
